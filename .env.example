# Development environment configuration

# Set to 'true' to simulate camera on non-Pi systems
SIMULATE_CAMERA=false

# Photo storage settings
PHOTO_DIR=data/photos
CAPTURE_INTERVAL=1
RETENTION_MINUTES=15
CLEANUP_INTERVAL=30

# Camera settings (for Phase 1+)
CAMERA_WIDTH=1920
CAMERA_HEIGHT=1080

# Detection settings (for Phase 2+)
# CONFIDENCE_THRESHOLD=0.5
# MODEL_PATH=models/yolov8n.pt

# Synthetic training data generation (for Phase 2B - OPTIONAL)
# Only needed if you want to generate synthetic training images
# Get your API key from: https://platform.openai.com/api-keys
# Cost: ~$0.04 per image (DALL-E 3 standard quality, 1024x1024)
# OPENAI_API_KEY=sk-your-key-here
# API_HOST=0.0.0.0
# API_PORT=8000
